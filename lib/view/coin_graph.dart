import 'package:flutter/widgets.dart';

class CoinGraphScreen extends StatelessWidget {
  final String idCoin;
  const CoinGraphScreen({@required this.idCoin}) : super();

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
